# 帳號與服務清單

本檔案記錄所有已知的伺服器服務與帳號資訊。
OpenCode 在需要操作任何服務時，應優先讀取本檔案取得資訊，不足時再詢問使用者。

---

## 使用方式

1. 將此檔案**複製**一份，命名為 `個人帳號與服務清單.md`
2. 將檔案放在 OpenCode 工作根目錄（即 Obsidian vault 根目錄）
3. 將下方所有 `<...>` 佔位符替換為你的實際帳號資訊
4. 設定 OpenCode 全域 AGENTS.md（見下方提示）

---



## SVN

- **URL**: `<http://your-svn-server/svn/repo>`
- **Username**: `<your-username>`
- **認證方式**: 第一次輸入後由作業系統快取（Windows Wincrypt / macOS Keychain）
- **支援操作**: checkout, update, commit, add, delete, move, status, diff, log, blame, cleanup, revert, info
- **本機目錄 (Windows)**: `<D:\workspace\svn-projects\>`
- **本機目錄 (macOS)**: `<~/Documents/svn-projects/>`

---

## GitLab

### 公司 GitLab

- **URL**: `<https://gitlab.your-company.com>`
- **Account username**: `<your-username>`
- **PAT Account**: `<your-username>`（PAT 已存在）
- **協定**: HTTPS + PAT
- **Credential 檔案**: `~/.git-credentials-gitlab-ovt`
- **SSL**: 自簽憑證（`insecureSkipVerify: true` / `SSL_VERIFY=false`）← 若為正式憑證請刪除此行
- **用途**: 公司內部專案開發

### GitLab.com（個人）

- **URL**: `https://gitlab.com`
- **Account username**: `<your-gitlab-username>`
- **PAT**: 已存在（透過 Git credential store 管理）
- **協定**: HTTPS 或 SSH（視網路環境而定）
- **用途**: 個人開源專案

---

## GitHub

### 主要帳號

- **Username**: `<your-main-github-username>`
- **PAT**: 透過 gh CLI 或 Token 管理
- **協作權限**: 所有懶人包 repo 的 admin

### 次要帳號（選填）

- **Username**: `<your-secondary-github-username>`
- **PAT**: 透過 gh CLI 或 Token 管理

---

## Feishu / Lark（飛書）

- **顯示名稱**: `<your-feishu-name>`
- **Email**: `<your-feishu-email>`
- **PAT**: 已存在
- **操作方式**: 透過 `feishu-inout` skill / Remote MCP 服務

---

## JIRA

- **URL**: `<https://jira.your-company.com>`
- **PAT**: 已設定於 `opencode.json` mcp-atlassian 環境變數
- **SSL**: 自簽憑證（`JIRA_SSL_VERIFY=false`）← 若為正式憑證請刪除此行
- **操作方式**: 透過 mcp-atlassian MCP 伺服器（`jira_*` 工具）
- **常用專案**: `<PROJ1>`、`<PROJ2>`
- **Issue Key 格式**: `專案代號-數字`，如 `<PROJ-123>`

---

## Confluence

- **URL**: `<https://confluence.your-company.com>`
- **PAT**: 已設定於 `opencode.json` mcp-atlassian 環境變數
- **SSL**: 自簽憑證（`CONFLUENCE_SSL_VERIFY=false`）← 若為正式憑證請刪除此行
- **操作方式**: 透過 mcp-atlassian MCP 伺服器（`confluence_*` 工具）
- **常用空間**: `<SPACE1>`、`<SPACE2>`

---

## Trac

- **app URL**: `https://10.0.0.77/trac/app`
- **app username**: `<your-app-username>`
- **app password**: 儲存於 `opencode.json` trac 環境變數的 `TRAC_APP_PASSWORD`
- **app_asic URL**: `https://10.0.0.77/trac/app_asic`
- **app_asic username**: `<your-asic-username>`
- **app_asic password**: 儲存於 `opencode.json` trac 環境變數的 `TRAC_APP_ASIC_PASSWORD`
- **SSL**: 自簽憑證（`TRAC_SSL_VERIFY=false`）
- **操作方式**: 透過 trac-mcp-server MCP 伺服器（`trac_*` 工具）
- **雙實例說明**: `app` 與 `app_asic` 的登入帳號可能不同（大小寫有區分）

---

## 操作規範

1. **優先讀取本檔案**：OpenCode 在需要服務資訊時，先透過 Obsidian MCP 讀取本檔案
2. **不足再問**：若資訊不足，詢問使用者後**寫回本檔案**，永久留存
3. **PAT 安全**：PAT 值僅存放於 `opencode.json` 的 MCP 環境變數中，**不寫入本檔案**
4. **新增 PAT**：若需新增 PAT，引導使用者至對應服務的 Settings → Access Tokens 手動產生
5. **多帳號選用**：若一個服務有多個帳號（如 GitHub、GitLab），操作前確認使用者要用哪一個

---

## 設定全域 AGENTS.md

建立 `~/.config/opencode/AGENTS.md`，內容如下：

```markdown
# 全域設定 — 個人帳號與服務清單

## 核心規則

在詢問使用者任何帳號/服務資訊之前，**必須先讀取以下檔案**：

```
<YOUR_OPENCODE_ROOT_DIR>/個人帳號與服務清單.md
```

讀取方式：直接讀取該檔案。

## 流程

1. 需要服務資訊時 → 讀取 `個人帳號與服務清單.md`
2. 資訊充足 → 直接使用，不詢問
3. 資訊不足 → 詢問使用者 → 寫回 `個人帳號與服務清單.md` 永久留存

## 安全規範

- PAT / Token 僅存放於 `~/.config/opencode/opencode.json` 的 MCP 環境變數中
- 不得將 PAT 原文寫入 `個人帳號與服務清單.md` 或任何 AGENTS.md
- 如需新增 PAT，引導使用者至服務網頁手動產生

## Obsidian Vault 關聯

- Vault 根目錄：`<YOUR_OPENCODE_ROOT_DIR>`
- 個人帳號清單：`個人帳號與服務清單.md`
```
