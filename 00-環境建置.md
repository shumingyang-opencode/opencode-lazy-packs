# OpenCode 懶人包 #00：環境建置

> 版本：v0.3
> 更新日期：2026-06-12

---

## 這個懶人包會幫你做什麼？

把使用 OpenCode 懶人包所需的環境一次建置完成：
- 安裝 Node.js（如果未安裝）
- 安裝 OpenCode
- 安裝 uv
- 驗證所有工具正常運作

> 💡 **本懶人包只負責安裝基礎工具。** Git、GitHub CLI、GitHub 帳號登入等需求，請接著執行 **懶人包 #02：連接 GitHub**（內含完整 Git/GitHub CLI 安裝）。

---

## 先備條件

- [ ] 電腦有網路連線

---

## 請 OpenCode 幫我執行以下步驟

> 把這份檔案的內容貼給 OpenCode，它會自動執行。

---

### 步驟一：環境檢查

請 OpenCode 先確認以下所有項目：

1. 作業系統：Windows / macOS / Linux
2. 網路連線
3. **Node.js**：`node --version`（需 18+）
4. **uv**（Python 工具管理器）：`uv --version`

把缺少的記錄下來，後續步驟逐一安裝。

---

### 步驟二：安裝 Node.js（如果未安裝或版本 < 18）

**Windows**：
```bash
winget install --id OpenJS.NodeJS --accept-source-agreements --accept-package-agreements
```

**macOS**：
```bash
brew install node
```

**Linux（Ubuntu/Debian）**：
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

確認：`node --version`

---

### 步驟三：安裝 OpenCode

```bash
npm install -g opencode-ai
```

確認版本：
```bash
opencode --version
```

---

### 步驟四：安裝 uv（如果未安裝）

**Windows（PowerShell）**：
```bash
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

**macOS / Linux**：
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

確認：`uv --version`

---

### 步驟五：最終驗證

```bash
node --version
opencode --version
uv --version
```

> ✅「環境建置完成！你的電腦已經準備好使用所有 OpenCode 懶人包了。」

### 步驟六：建立個人帳號清單（首次執行）

建立 `個人帳號與服務清單.md` 於 OpenCode 根目錄（即未來 Obsidian vault 根目錄），作為所有服務資訊的中央檔案，避免每次重複詢問。

若此檔案已存在，則跳過此步驟。若不存在，詢問使用者以下資訊後建立：
1. GitHub 帳號名稱
2. GitLab 帳號名稱與伺服器網址
3. 其他已知服務資訊

> 此檔案會被 OpenCode 全域 AGENTS.md 參考，建議同時將 Obsidian vault 設為同一目錄（詳見懶人包 #06）。

---

## 完成回報格式

```md
## 環境建置完成

- Node.js：v<版本> ✅ / ⚠️ 已補裝
- OpenCode：v<版本> ✅ / ⚠️ 已補裝
- uv：v<版本> ✅ / ⚠️ 已補裝
```

---

## 常見問題

| 問題 | 解法 |
|------|------|
| `opencode` 指令找不到 | 確認 npm global bin 在 PATH 中：`npm config get prefix`，把 `prefix/bin` 加入 PATH |
| winget 找不到 | Windows 限定：Windows 10 1809+ 內建；或到 Microsoft Store 安裝「App Installer」。macOS 請用 `brew install node` |
| Node.js 版本太舊 | 移除舊版重裝最新 LTS |

---

## 解除安裝

### 移除 CLI 工具

```bash
npm uninstall -g opencode-ai
```

---

## Trae 對應操作

> 若你使用 **Trae IDE**，安裝方式與 OpenCode 完全相同。
> 所有 CLI 工具（npm、pipx、brew 等）在 Trae 環境中可直接使用。

### 在 Trae 上安裝

與上方步驟完全相同，無需額外設定。

### 在 Trae 上更新

與 OpenCode 相同，重新執行對應的套件管理器指令。

### 在 Trae 上移除

與 OpenCode 相同。

---

## 更新紀錄

| 日期 | 版本 | 更新內容 |
|------|------|---------|
| 2026-06-12 | v0.3 | 移除 Git/GitHub CLI 安裝（改由 #02 負責），#00 只裝 Node.js + OpenCode + uv |
| 2026-05-19 | v0.1 | 初版 |
