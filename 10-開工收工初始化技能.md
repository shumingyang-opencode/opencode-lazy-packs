# OpenCode 懶人包 #10：開工/收工/初始化技能

> 版本：v2.0
> 更新日期：2026-07-23

---

## 這個懶人包會幫你做什麼？

安裝 OpenCode 全域技能（SKILL.md），讓你的開發工作流程自動化：

- **startup（開工）**：說「開工」→ 自動判斷儲存模式（Obsidian / 本機）+ 首次設定流程 + 多 remote pull + 讀 session 紀錄
- **shutdown（收工）**：說「收工」→ 自動 Git commit/push + 寫 Dataview 相容 session 區塊 + （Obsidian 模式）wikilink + 知識庫彙整頁面 + 多 remote push
- **project-init（專案初始化）**：說「初始化專案」→ 自動建 AGENTS.md + Git + 知識庫同步設定
- **週報發送（附加功能）**：說「發送週報」→ 自動彙整每日 session 進度 → Outlook 寄送
- **convert-to-obsidian（知識庫轉換）**：說「轉換知識庫」→ 本機→Obsidian 轉換；說「更新 session 格式」→ 舊格式更新為 Dataview 相容

### v2.0 新增功能

| 功能 | 說明 |
|------|------|
| **雙儲存模式** | 支援 Obsidian vault 或本機目錄，startup/shutdown 根據設定自動選擇讀寫方式 |
| **首次設定流程** | 首次開工若無 sync config，引導使用者選擇儲存模式 + 同步平台 |
| **多平台同步** | 支援 GitHub / 公司 GitLab / Both（鏡像）/ 本機 only 四種選擇 |
| **Dataview 相容格式** | Session 區塊使用 `- type:: session` / `- 討論::` inline field，可在 Obsidian 中用 Dataview 查詢 |
| **知識庫彙整頁面** | [Obsidian 模式] 自動建立 `知識庫/工作紀錄總覽.md`，含 Dataview 查詢語法 |
| **Wikilink 關聯** | [Obsidian 模式] 每個 session 自動加入 `相關專案：[[projects/專案名]]` |
| **多 remote push** | shutdown 對所有設定的 remote 逐一 push，失敗不中斷 |
| **舊格式轉換** | convert-to-obsidian 技能可批次將舊格式 `討論：` 更新為 Dataview 相容格式 |

---

## 儲存模式說明

本懶人包 v2.0 支援兩種儲存模式，startup/shutdown 在執行時讀取設定自動選擇對應操作：

| 面向 | Obsidian 模式 | 本機模式 |
|------|--------------|---------|
| 每日筆記位置 | Vault 內 `每日筆記/` | `$SESSION_DIR/每日筆記/` |
| 週報位置 | Vault 內 `週報/` | `$SESSION_DIR/週報/` |
| 讀寫方式 | `obsidian_read_note` / `obsidian_patch_note` | `read` / `edit` / `write` |
| Frontmatter | `obsidian_update_frontmatter` | 直接編輯 YAML |
| 搜尋 | `obsidian_search_notes` | `grep` |
| Git 同步 | `git -C <vault_path>` | `git -C $SESSION_DIR` |
| 額外功能 | Wikilink + Dataview 彙整頁面 | 無 |

### 推薦用法

- **有安裝 Obsidian** → 選 Obsidian 模式，享受 wikilink 關聯 + Dataview 查詢 + 本機搜尋
- **無安裝 Obsidian** → 選本機模式，指定一個目錄存放 session 紀錄，之後可隨時用 `convert-to-obsidian` 技能搬遷

---

## 同步平台選擇

首次設定時可選擇知識庫的版本控管平台：

| 選擇 | Repo 建立方式 | 使用情境 |
|------|-------------|---------|
| **GitHub** | `gh repo create --private` | 個人 / 開源 |
| **公司 GitLab** | GitLab REST API（用既有 PAT） | 公司內部 |
| **Both（鏡像）** | 兩個平台都建立 | 同時需要公司內部 + 個人備份 |
| **本機 only** | 不建立遠端 repo | 純本地使用，無跨機器需求 |

選擇 Both 時，shutdown 會同時 push 到 GitHub 和 GitLab，任一失敗不中斷。

---

## 先備條件

- [ ] GitHub CLI 已登入（若選擇 GitHub 平台）
- [ ] GitLab PAT 已存在（若選擇公司 GitLab 平台）— 已存在於 MCP 設定中
- [ ] （可選）Obsidian 已安裝 + MCP 已設定
- [ ] （可選）Obsidian Dataview 社群外掛 — 知識庫彙整頁面需要

---

## 請 OpenCode 幫我執行以下步驟

### 步驟一：建立 skills 目錄

```bash
mkdir -p ~/.config/opencode/skills/startup
mkdir -p ~/.config/opencode/skills/shutdown
mkdir -p ~/.config/opencode/skills/project-init
mkdir -p ~/.config/opencode/skills/weekly-report
mkdir -p ~/.config/opencode/skills/convert-to-obsidian
```

### 步驟二：建立 startup 技能

建立 `~/.config/opencode/skills/startup/SKILL.md`：

```markdown
---
name: startup
description: 開工自動同步 — 說「開工」「我來了」「上次做到哪」時載入
---

當使用者說「開工」時，依序執行：

### 步驟 0：讀取知識庫設定

讀取 `個人帳號與服務清單.md`，尋找 `### daily-notes` 區塊：

- 若不存在 → 啟動**首次設定流程**（步驟 0a）
- 若存在 → 確認 `儲存模式` 和 `Local Path`，繼續步驟 1

#### 步驟 0a：首次設定流程

1. **選擇儲存模式**
   - 選項 A：使用 Obsidian（需已安裝 Obsidian MCP）
   - 選項 B：本機模式（自行指定目錄）
   - 選項 C：跳過設定（每次開工都提醒直到完成）

2. **若選 Obsidian** — 嘗試 `obsidian_get_vault_stats` 確認是否可用
   - 可用 → 自動使用 vault 根目錄為 `Local Path`
   - 不可用 → 提示錯誤，回到選項

3. **若選本機模式** — 請使用者輸入完整目錄路徑，建立目錄

4. **選擇同步平台**
   - 選項 A：GitHub
   - 選項 B：公司 GitLab（`gitlab.ovt.com:8081`）
   - 選項 C：Both（鏡像）
   - 選項 D：本機 only（不建立遠端 repo）

5. **建立遠端 repo** — 依所選平台建立私有 repo
   - GitHub → `gh repo create work-notes --private`
   - 公司 GitLab → GitLab REST API

6. **Git 初始化** — `git init` + `.gitignore` + initial commit

7. **設定 remote + push** — 依平台加入對應 remote 並 push

8. **寫入 sync config 至個人帳號與服務清單.md**

### 步驟 1：Git 同步

依序 pull 所有 remote，失敗不中斷。有 conflict 則提示手動處理。

### 步驟 2-7：判斷專案 → 讀每日筆記 → 搜尋 session → 記錄時間 → Git 檢查 → 回報

根據儲存模式選擇對應的讀寫工具。
```

### 步驟三：建立 shutdown 技能

建立 `~/.config/opencode/skills/shutdown/SKILL.md`：

```markdown
---
name: shutdown
description: 收工自動同步 — 說「收工」「下班」「結束」時載入
---

### 步驟 0：讀取知識庫設定
### 步驟 1：專案 Git 同步
### 步驟 2：敏感檔案提醒
### 步驟 3：Session 寫入

3a. 取得 commit 摘要（`git log --oneline --since=<開工時間>`）
3b. 根據儲存模式讀取每日筆記
3c. 產生 Dataview 相容格式 session 區塊：

```
### S<N> (HH:mm-HH:mm)

- type:: session
- project:: {{project_name}}
- commit:: `abc123`
- 討論:: AI 總結
- 問題:: 卡關問題
- 解決:: 解決方案
- 下一步:: 待辦
```

3d. 根據儲存模式寫入：
   - Obsidian → `obsidian_patch_note` + wikilink `相關專案：[[projects/專案名]]`
   - 本機 → `edit` 直接寫入檔案
3e. 更新 frontmatter 的 `updated` 時間
3f. [Obsidian 專屬] 建立 `知識庫/工作紀錄總覽.md`（含 Dataview 語法）

### 步驟 4：Git 同步知識庫

`git add` → `git commit` → 逐一 push 所有 remote

### 步驟 5：檢查舊格式 session

### 步驟 6：最終 checklist 回報
```

### 步驟四：建立 project-init 技能

建立 `~/.config/opencode/skills/project-init/SKILL.md`：

完整流程：
1. 收集專案資訊（名稱、簡介、技術棧）
2. 從模板複製 AGENTS.md + rules/
3. 產生 minimal `opencode.json`（key 用 `"command"`，非 `"customCommands"`）
4. Git init + `.gitignore` + initial commit
5. **設定知識庫同步**（v2.0 新增）：
   - 讀取現有 sync config（若有則跳過）
   - 選擇儲存模式（Obsidian / 本機 / 跳過）
   - 選擇同步平台（GitHub / 公司 GitLab / Both / 本機 only）
   - 建立遠端 repo + git remote + push
   - 寫入 sync config 至 `個人帳號與服務清單.md`
6. 更新 AGENTS.md 同步對照表
7. 詢問是否進行功能規劃（載入 `project-plan-feature` 技能）
8. 完成報告

### 步驟四之一：建立週報發送技能（附加功能）

建立 `~/.config/opencode/skills/weekly-report/SKILL.md`：

此功能依賴 startup 產出的 session 資料。v2.0 支援雙路徑讀取：

1. 檢查 `config.json` 的 recipients / last_sent
2. 讀取 `個人帳號與服務清單.md` 的儲存模式與 Local Path
3. 根據儲存模式掃描每日筆記（Obsidian → `obsidian_read_note`，本機 → `read`）
4. 解析 session 區塊（支援新舊兩種格式）
5. 彙整摘要 → 產出週報內容
6. 根據模式存檔週報
7. 顯示給使用者確認 → Outlook COM 發送
8. 更新 last_sent

建立 `~/.config/opencode/skills/weekly-report/config.json`：

```json
{
  "recipients": [],
  "last_sent": "2026-07-10T00:00:00"
}
```

### 步驟四之二：建立知識庫轉換技能（v2.0 新增）

建立 `~/.config/opencode/skills/convert-to-obsidian/SKILL.md`：

雙模式技能：

**模式 A：本機 → Obsidian 轉換**
- 確認 Obsidian MCP 可用
- 讀取本機 session 目錄
- 複製每日筆記 + 週報至 vault
- 轉換 session 格式為 Dataview 相容
- 建立知識庫彙整頁面
- 更新 sync config（儲存模式 → obsidian）
- Git 初始化 vault
- 可選刪除舊目錄

**模式 B：更新舊格式 session**
- 掃描每日筆記
- 將 `討論：` → `- 討論::` 等舊格式轉為 inline field 格式
- 補上 `- type:: session`
- 補上 `- project:: 專案名`
- 寫回檔案

### 步驟五：寫入 opencode.json 權限設定

> ⚠️ **已知陷阱：** `project-init` 技能在產生 `opencode.json` 時，key 必須使用 `"command"`，**不可使用 `"customCommands"`**。

確認 `~/.config/opencode/opencode.json` 中有：

```json
{
  "permission": {
    "skill": {
      "startup": "allow",
      "shutdown": "allow",
      "project-init": "allow",
      "weekly-report": "allow",
      "convert-to-obsidian": "allow",
      "*": "ask"
    }
  }
}
```

### 步驟六：驗證技能

重新開啟 OpenCode，執行以下驗證：
1. 說「開工」→ 確認首次設定流程被觸發（若無 sync config）
2. 選擇儲存模式 + 同步平台，完成設定
3. 說「收工」→ 確認 session 寫入 + Git push
4. 說「轉換知識庫」→ 確認技能載入（可選）

---

## 完成回報格式

```md
## 五技能安裝完成

- startup：~/.config/opencode/skills/startup/SKILL.md ✅
- shutdown：~/.config/opencode/skills/shutdown/SKILL.md ✅
- project-init：~/.config/opencode/skills/project-init/SKILL.md ✅
- weekly-report：~/.config/opencode/skills/weekly-report/SKILL.md + config.json ✅
- convert-to-obsidian：~/.config/opencode/skills/convert-to-obsidian/SKILL.md ✅
- opencode.json 權限：已設定
- 儲存模式：Obsidian / 本機（依使用者選擇）
- 同步平台：GitHub / 公司 GitLab / Both / 本機 only（依使用者選擇）
- 驗證：待重啟 OpenCode
```

---

## 常見問題

| 問題 | 解法 |
|------|------|
| skill 沒出現在可用清單 | 確認 SKILL.md 檔名全大寫、frontmatter 格式正確 |
| 「開工」無反應 | 重新開啟 OpenCode 讓它掃描技能目錄 |
| 首次設定選錯模式 | 編輯 `個人帳號與服務清單.md` 的 `儲存模式` 改回來，或說「轉換知識庫」搬遷 |
| 本機模式想轉 Obsidian | 安裝 Obsidian + 設定 MCP → 說「轉換知識庫」 |
| 舊 session 無法在 Dataview 查到 | 說「更新 session 格式」批次轉換 |
| GitLab push 失敗 | 確認 PAT 有效、網路通、SSL 設定正確（公司 GitLab 需 `insecureSkipVerify: true`） |

---

## 解除安裝

### 移除 Skill

```bash
rm -rf ~/.config/opencode/skills/startup/
rm -rf ~/.config/opencode/skills/shutdown/
rm -rf ~/.config/opencode/skills/project-init/
rm -rf ~/.config/opencode/skills/weekly-report/
rm -rf ~/.config/opencode/skills/convert-to-obsidian/
```

### 移除 Permission

編輯 `~/.config/opencode/opencode.json`，從 `"permission"` 的 `"skill"` 區塊移除對應的 allow entry。

---

## 更新紀錄

| 日期 | 版本 | 更新內容 |
|------|------|---------|
| 2026-05-19 | v0.1 | 初版 |
| 2026-07-17 | v0.2 | 改版：startup 支援多專案 session 判別 + 記錄系統時間；shutdown 補結構化 session 區塊 |
| 2026-07-17 | v0.3 | 新增：週報發送附加功能（weekly-report） |
| 2026-07-20 | v0.4 | 新增：startup/shutdown 的 Obsidian vault Git 同步，支援多 remote 鏡像 push |
| 2026-07-21 | v0.5 | 新增：shutdown 下一步選項 C「結束當前任務」 |
| 2026-07-21 | v0.6 | 移除：shutdown 的 chezmoi 同步步驟 |
| **2026-07-23** | **v2.0** | **大改版：雙儲存模式（Obsidian / 本機）、首次設定流程、多平台同步（GitHub / 公司 GitLab / Both / 本機 only）、Dataview 相容 session 格式、wikilink 關聯、知識庫彙整頁面、convert-to-obsidian 轉換技能** |
