# OpenCode 懶人包 #10：開工/收工/初始化技能

> 版本：v0.2
> 更新日期：2026-07-17

---

## 這個懶人包會幫你做什麼？

安裝 OpenCode 全域技能（SKILL.md），讓你的開發工作流程自動化：

- **startup（開工）**：說「開工」→ 自動判斷當前專案 + 讀 Obsidian session 紀錄 + 檢查 Git 狀態
- **shutdown（收工）**：說「收工」→ 自動 Git commit/push + 補結構化 session 區塊至 Obsidian + chezmoi 同步
- **project-init（專案初始化）**：說「初始化專案」→ 自動建 AGENTS.md + Git + GitHub + Obsidian

---

## 先備條件

- [ ] 已完成 **懶人包 #06：建立第二大腦 Obsidian**
- [ ] GitHub CLI 已登入

---

## 請 OpenCode 幫我執行以下步驟

### 步驟一：建立 skills 目錄

```bash
mkdir -p ~/.config/opencode/skills/startup
mkdir -p ~/.config/opencode/skills/shutdown
mkdir -p ~/.config/opencode/skills/project-init
```

### 步驟二：建立 startup 技能

建立 `~/.config/opencode/skills/startup/SKILL.md`：

```markdown
---
name: startup
description: 開工自動同步 — 說「開工」「我來了」「上次做到哪」時載入
---

當使用者說「開工」時，依序執行：

1. 從 git remote 或工作目錄名稱判斷**當前專案**
2. 讀取 Obsidian `每日筆記/<今日日期>.md`
   - 若筆記不存在 → 建立（含 frontmatter: date, updated）
3. 在筆記中搜尋當前專案的最後一個 `### S` session 區塊
   - 若有 → 提取「下一步」作為建議
   - 若無 → 首次開工，提示無上次狀態
4. 記錄**目前系統時間**作為本次 session 起始時間
5. 檢查本地 git status + 遠端新 commit
6. 回報結構化摘要 + 建議下一步

**原則**：開工只讀不寫。唯一例外是筆記不存在時才建立空筆記。
```

### 步驟三：建立 shutdown 技能

建立 `~/.config/opencode/skills/shutdown/SKILL.md`：

```markdown
---
name: shutdown
description: 收工自動同步 — 說「收工」「下班」「結束」時載入
---

當使用者說「收工」時，依序執行：

1. **專案 Git 同步**（commit + push，等使用者確認 commit 訊息）
2. **chezmoi 同步**（`~/.config/opencode/` 或共用 dotfiles 設定變動）
3. **敏感檔案提醒**（API key 等）
4. **Obsidian 更新** — 補 session 區塊

   a. 用 `git log --oneline --since=<開工系統時間>` 取得本次 session commit 摘要
   b. 讀取 `每日筆記/<今日日期>.md`
   c. 在當前專案標題下 append 新 session 區塊：

      ```
      ### S<N> (HH:mm-HH:mm)
      commit: `abc123` `def456`
      討論：<AI 總結本次 session 工作內容>
      問題：<AI 提取的卡關/問題>
      解決：<AI 提取的解決方案>
      下一步：<簡述>
      ```

      - `N` = 數該專案已有幾個 `### S`，+1
      - 時間區間 = 開工系統時間 → 現在
      - `commit:` 由 `git log --oneline --since=<開工時間>` 取得
      - `討論：` `問題：` `解決：` 由 AI 根據 session 記憶自動產出，該欄位無內容則省略該行
      - `下一步：` 收工後詢問使用者：選項 A 自行輸入 / 選項 B 直接終止（寫「待確認」）
      - 未 commit 的變更要提醒使用者先 commit 或 stash

   d. 用 `obsidian_update_frontmatter` 更新 `updated` 時間

5. **最終 checklist 回報**
```

### 步驟四：建立 project-init 技能

建立 `~/.config/opencode/skills/project-init/SKILL.md`：

```markdown
---
name: project-init
description: 新專案初始化 — 說「初始化專案」時載入
---

當使用者說「初始化專案」時，依序執行：
1. 建立 AGENTS.md（含專案名稱、簡介、資料夾結構）
2. Git init + .gitignore + initial commit
3. GitHub 私有 repo 建立 + push
4. Obsidian 對應資料夾與工作流程筆記建立
5. 更新 AGENTS.md 同步對照表
6. 詢問是否進行功能規劃（載入 project-plan-feature 技能）
7. 完成報告
```

### 步驟五：寫入 opencode.json 權限設定

> ⚠️ **已知陷阱：** `project-init` 技能在產生 `opencode.json` 時，key 必須使用 `"command"`，**不可使用 `"customCommands"`**（OpenCode IDE Config schema 不支援 `customCommands`）。若安裝後發生 `ConfigInvalidError`，請檢查 `opencode.json` 中是否誤用了 `customCommands`。

確認 `~/.config/opencode/opencode.json` 中有：

```json
{
  "permission": {
    "skill": {
      "startup": "allow",
      "shutdown": "allow",
      "project-init": "allow",
      "*": "ask"
    }
  }
}
```

### 步驟六：驗證技能

重新開啟 OpenCode，然後說「開工」，確認技能被載入。

---

## 完成回報格式

```md
## 三大技能安裝完成

- startup：~/.config/opencode/skills/startup/SKILL.md ✅
- shutdown：~/.config/opencode/skills/shutdown/SKILL.md ✅
- project-init：~/.config/opencode/skills/project-init/SKILL.md ✅
- opencode.json 權限：已設定
- 驗證：待重啟 OpenCode
```

---

## 常見問題

| 問題 | 解法 |
|------|------|
| skill 沒出現在可用清單 | 確認 SKILL.md 檔名全大寫、frontmatter 格式正確 |
| 「開工」無反應 | 重新開啟 OpenCode 讓它掃描技能目錄 |

---

## 解除安裝

### 移除 Skill

```bash
rm -rf ~/.config/opencode/skills/startup/
rm -rf ~/.config/opencode/skills/shutdown/
rm -rf ~/.config/opencode/skills/project-init/
```

### 移除 Permission

編輯 `~/.config/opencode/opencode.json`，從 `"permission"` 的 `"skill"` 區塊移除 `"startup": "allow"`、`"shutdown": "allow"`、`"project-init": "allow"`。

---

## Trae 對應操作

> 若你使用 **Trae IDE**，以下為對應的安裝/更新/移除步驟。

### 在 Trae 上安裝

1. 將三個技能分別放入 `.trae/skills/` 目錄：
   ```bash
   mkdir -p .trae/skills/startup/ .trae/skills/shutdown/ .trae/skills/project-init/
   ```
2. 從本 repo 的 `skills/10-startup-shutdown-init/` 複製各 `SKILL.md` 到對應目錄
3. 重新載入 Trae（Cmd+R / Ctrl+R）
4. 對 Trae 說「開工」「收工」「初始化專案」，確認可載入

### 在 Trae 上更新

替換 `.trae/skills/<name>/SKILL.md` 的內容即可。

### 在 Trae 上移除

```bash
rm -rf .trae/skills/startup/ .trae/skills/shutdown/ .trae/skills/project-init/
```

---

## 更新紀錄

| 日期 | 版本 | 更新內容 |
|------|------|---------|
| 2026-05-19 | v0.1 | 初版 |
| 2026-07-17 | v0.2 | 改版：startup 支援多專案 session 判別 + 記錄系統時間；shutdown 補結構化 session 區塊（討論/問題/解決/下一步） |
