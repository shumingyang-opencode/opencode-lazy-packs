# OpenCode 懶人包 #10：開工/收工/初始化技能

> 版本：v0.4
> 更新日期：2026-07-20

---

## 這個懶人包會幫你做什麼？

安裝 OpenCode 全域技能（SKILL.md），讓你的開發工作流程自動化：

- **startup（開工）**：說「開工」→ 自動判斷當前專案 + 讀 Obsidian session 紀錄 + 檢查 Git 狀態
- **shutdown（收工）**：說「收工」→ 自動 Git commit/push + 補結構化 session 區塊至 Obsidian + vault Git sync
- **project-init（專案初始化）**：說「初始化專案」→ 自動建 AGENTS.md + Git + GitHub + Obsidian
- **週報發送（附加功能）**：說「發送週報」→ 自動彙整每日 session 進度 → Outlook 寄送

---

## 先備條件

- [ ] 已完成 **懶人包 #06：建立第二大腦 Obsidian**
- [ ] GitHub CLI 已登入

---

## 請 OpenCode 幫我執行以下步驟

### 步驟一：建立 skills 目錄

```bash
mkdir -p ~/.agents/skills/startup
mkdir -p ~/.agents/skills/shutdown
mkdir -p ~/.agents/skills/project-init
```

### 步驟二：建立 startup 技能

建立 `~/.agents/skills/startup/SKILL.md`：

```markdown
---
name: startup
description: 開工自動同步 — 說「開工」「我來了」「上次做到哪」時載入
---

當使用者說「開工」時，依序執行：

0. **Git 同步 Obsidian vault**
   - 讀取 `個人帳號與服務清單.md` 確認 daily-notes 的 sync config
   - 若有設定 → `git -C <OBSIDIAN_VAULT_PATH> pull --rebase`
   - 有 conflict → 提示使用者手動處理後重試，中止開工流程

1. 從 git remote 或工作目錄名稱判斷**當前專案**
2. 讀取 Obsidian `每日筆記/<今日日期>.md`
   - 若筆記不存在 → 建立（含 frontmatter: date, updated）
3. 在筆記中搜尋當前專案的最後一個 `### S` session 區塊
   - 若有 → 提取「下一步」作為建議
   - 若無 → 首次開工，提示無上次狀態
4. 記錄**目前系統時間**作為本次 session 起始時間
5. 檢查本地 git status + 遠端新 commit
6. 回報結構化摘要 + 建議下一步

**原則**：開工只讀不寫。唯一例外是筆記不存在時才建立空筆記。
```

### 步驟三：建立 shutdown 技能

建立 `~/.agents/skills/shutdown/SKILL.md`：

```markdown
---
name: shutdown
description: 收工自動同步 — 說「收工」「下班」「結束」時載入
---

當使用者說「收工」時，依序執行：

1. **專案 Git 同步**（commit + push，等使用者確認 commit 訊息）
2. **敏感檔案提醒**（API key 等）
3. **Obsidian 更新** — 補 session 區塊

   a. 用 `git log --oneline --since=<開工系統時間>` 取得本次 session commit 摘要
   b. 讀取 `每日筆記/<今日日期>.md`
   c. 在當前專案標題下 append 新 session 區塊：

      ```
      ### S<N> (HH:mm-HH:mm)
      commit: `abc123` `def456`
      討論：<AI 總結本次 session 工作內容>
      問題：<AI 提取的卡關/問題>
      解決：<AI 提取的解決方案>
      下一步：<簡述>
      ```

      - `N` = 數該專案已有幾個 `### S`，+1
      - 時間區間 = 開工系統時間 → 現在
      - `commit:` 由 `git log --oneline --since=<開工時間>` 取得
      - `討論：` `問題：` `解決：` 由 AI 根據 session 記憶自動產出，該欄位無內容則省略該行
      - `下一步：` 收工後詢問使用者：選項 A 自行輸入 / 選項 B 直接終止（寫「待確認」）/ 選項 C 結束當前任務（寫「結束當前任務」）
      - 未 commit 的變更要提醒使用者先 commit 或 stash

   d. 用 `obsidian_update_frontmatter` 更新 `updated` 時間

4. **Git 同步 Obsidian vault**
   - 讀取 `個人帳號與服務清單.md` 確認 daily-notes 的 sync config
   - 若有設定 → `git -C <OBSIDIAN_VAULT_PATH> add -A`
   - 根據 session 內容自動產生 commit message：

     ```
     每日筆記 <日期>

     - <討論/問題/解決/下一步 有內容的欄位各縮寫成一行>
     ```

   - `git -C <OBSIDIAN_VAULT_PATH> commit -m "<message>"`
   - 逐個 push 到所有 remote：`git -C <OBSIDIAN_VAULT_PATH> push origin main`
     - 若有多個 remote 則逐一 push
     - 任一 push 失敗 → 列出失敗的 remote，提醒使用者

5. **最終 checklist 回報**
```

### 步驟四：建立 project-init 技能

建立 `~/.agents/skills/project-init/SKILL.md`：

```markdown
---
name: project-init
description: 新專案初始化 — 說「初始化專案」時載入
---

當使用者說「初始化專案」時，依序執行：
1. 建立 AGENTS.md（含專案名稱、簡介、資料夾結構）
2. Git init + .gitignore + initial commit
3. GitHub 私有 repo 建立 + push
4. Obsidian 對應資料夾與工作流程筆記建立
5. 更新 AGENTS.md 同步對照表
6. 詢問是否進行功能規劃（載入 project-plan-feature 技能）
7. 完成報告
```

### 步驟四之一：安裝週報發送技能（附加功能）

此功能依賴 startup 產出的 session 資料，每次「發送週報」時自動彙整並寄送。

建立 `~/.agents/skills/weekly-report/SKILL.md`：

```markdown
---
name: 週報發送
description: 彙整每週進度並發送報告 — 說「發送週報」「weekly report」時載入
---

當使用者說「發送週報」時，依序執行：

1. **檢查設定**
   - 讀取同目錄下 `config.json`
   - 若 `recipients` 為空 → 詢問使用者輸入收信者 Email 清單（逗號分隔）→ 寫入
   - 讀取 `last_sent`（首次安裝預設為 7 天前的日期）

2. **掃描 Obsidian 每日筆記**
   - 從 `last_sent` 到今天的每一天
   - 讀取 `每日筆記/<日期>.md`
   - 搜尋 `### S<N> (HH:mm-HH:mm)` 區塊，解析每筆 session
   - 按專案分組（第二層標題 `## 專案名`）

3. **彙整摘要 → 產出週報內容**
   - 每專案：session 數、commit 數、討論焦點、待解決問題、下一步
   - 產出完整週報 Markdown 字串

4. **存檔至 Obsidian**
   - 寫入 `週報/<年>-W<週數>.md`
   - frontmatter: `{ date, week, tags: [週報] }`

5. **打印給使用者確認**
   - 顯示週報內容
   - 詢問：發送(Y) / 修改(E) / 取消(N)

6. **使用者確認後 → Outlook COM 發送**
   - 將週報寫入暫存 `weekly-report.md`
   - 執行 PowerShell：
     ```powershell
     $ol = New-Object -ComObject Outlook.Application
     $mail = $ol.CreateItem(0)
     $mail.To = "<recipients 分號串接>"
     $mail.Subject = "週報 <年>-W<週數>"
     $mail.Attachments.Add("<暫存路徑>\\weekly-report.md")
     $mail.Display()
     ```
   - 讓使用者在 Outlook 中做最後確認後按送出
   - ⚠ 若 `New-Object` 失敗（無 Outlook）→ 存至桌面，告知手動寄送

7. **更新 config.json** 的 `last_sent` 為本次發送時間

8. **完成回報**
```

建立 `~/.agents/skills/weekly-report/config.json`：

```json
{
  "recipients": [],
  "last_sent": "2026-07-10T00:00:00"
}
```

建立 Obsidian 週報歸檔資料夾：

```bash
mkdir -p <OBSIDIAN_VAULT_ROOT>/週報
```

### 步驟五：寫入 opencode.json 權限設定

> ⚠️ **已知陷阱：** `project-init` 技能在產生 `opencode.json` 時，key 必須使用 `"command"`，**不可使用 `"customCommands"`**（OpenCode IDE Config schema 不支援 `customCommands`）。若安裝後發生 `ConfigInvalidError`，請檢查 `opencode.json` 中是否誤用了 `customCommands`。

確認 `~/.config/opencode/opencode.json` 中有：

```json
{
  "permission": {
    "skill": {
      "startup": "allow",
      "shutdown": "allow",
      "project-init": "allow",
      "weekly-report": "allow",
      "*": "ask"
    }
  }
}
```

### 步驟六：驗證技能

重新開啟 OpenCode，然後說「開工」，確認技能被載入。

---

## 完成回報格式

```md
## 三大技能 + 週報發送安裝完成

- startup：~/.agents/skills/startup/SKILL.md ✅
- shutdown：~/.agents/skills/shutdown/SKILL.md ✅
- project-init：~/.agents/skills/project-init/SKILL.md ✅
- weekly-report：~/.agents/skills/weekly-report/SKILL.md ✅
- opencode.json 權限：已設定
- Obsidian 週報資料夾：已建立
- 驗證：待重啟 OpenCode
```

---

## 常見問題

| 問題 | 解法 |
|------|------|
| skill 沒出現在可用清單 | 確認 SKILL.md 檔名全大寫、frontmatter 格式正確 |
| 「開工」無反應 | 重新開啟 OpenCode 讓它掃描技能目錄 |

---

## 解除安裝

### 移除 Skill

```bash
rm -rf ~/.agents/skills/startup/
rm -rf ~/.agents/skills/shutdown/
rm -rf ~/.agents/skills/project-init/
rm -rf ~/.agents/skills/weekly-report/
```

### 移除 Permission

編輯 `~/.config/opencode/opencode.json`，從 `"permission"` 的 `"skill"` 區塊移除 `"startup": "allow"`、`"shutdown": "allow"`、`"project-init": "allow"`、`"weekly-report": "allow"`。

---

## Trae 對應操作

> 若你使用 **Trae IDE**，以下為對應的安裝/更新/移除步驟。

### 在 Trae 上安裝

1. 選擇安裝層級：
   - **全域（推薦，所有專案可用）**：`~/.agents/skills/<name>/`
   - **專案（僅當前專案）**：`.agents/skills/<name>/`

   ```bash
   # 全域
   mkdir -p ~/.agents/skills/startup/ ~/.agents/skills/shutdown/ ~/.agents/skills/project-init/ ~/.agents/skills/weekly-report/
   # 或專案
   mkdir -p .agents/skills/startup/ .agents/skills/shutdown/ .agents/skills/project-init/ .agents/skills/weekly-report/
   ```
2. 從本 repo 的 `skills/10-startup-shutdown-init/` 複製各 `SKILL.md` 到對應目錄
3. 重新載入 Trae（Cmd+R / Ctrl+R）
4. 對 Trae 說「開工」「收工」「初始化專案」，確認可載入

### 在 Trae 上更新

替換對應目錄中的 `SKILL.md` 內容即可：
- 全域：`~/.agents/skills/<name>/SKILL.md`
- 專案：`.agents/skills/<name>/SKILL.md`

### 在 Trae 上移除

```bash
# 全域
rm -rf ~/.agents/skills/startup/ ~/.agents/skills/shutdown/ ~/.agents/skills/project-init/ ~/.agents/skills/weekly-report/
# 或專案
rm -rf .agents/skills/startup/ .agents/skills/shutdown/ .agents/skills/project-init/ .agents/skills/weekly-report/
```

---

## 更新紀錄

| 日期 | 版本 | 更新內容 |
|------|------|---------|
| 2026-05-19 | v0.1 | 初版 |
| 2026-07-17 | v0.2 | 改版：startup 支援多專案 session 判別 + 記錄系統時間；shutdown 補結構化 session 區塊（討論/問題/解決/下一步） |
| 2026-07-17 | v0.3 | 新增：週報發送附加功能（weekly-report），自動彙整 session 進度 + Outlook COM 寄送 |
| 2026-07-20 | v0.4 | 新增：startup/shutdown 的 Obsidian vault Git 同步（跨機器 sync config），支援多 remote 鏡像 push |
| 2026-07-21 | v0.5 | 新增：shutdown 下一步選項 C「結束當前任務」，取代原本只能寫「待確認」|
| 2026-07-21 | v0.6 | 移除：shutdown 的 chezmoi 同步步驟（未安裝 chezmoi 時跳過無意義）|
